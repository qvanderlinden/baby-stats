name: baby_stats

services:
  db:
    image: postgres:17.6-alpine
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB__DATABASE}
      POSTGRES_USER: ${POSTGRES_DB__USER}
      POSTGRES_PASSWORD: ${POSTGRES_DB__PASSWORD}
    ports:
      - "${POSTGRES_DB__PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  adminer:
    image: adminer
    restart: always
    ports:
      - "${ADMINER_PORT}:8080"

volumes:
  postgres_data:
